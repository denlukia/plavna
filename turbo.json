{
	"$schema": "https://turbo.build/schema.json",
	"globalDependencies": [".env"],
	"tasks": {
		"build": {
			"dependsOn": ["^build"],
			"outputs": [".svelte-kit/**"]
		},
		"dev": {
			"persistent": true,
			"cache": false
		}
	}
}
